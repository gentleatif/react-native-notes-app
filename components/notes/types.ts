export type Note = {
  id: number;
  title: string;
  description: string;
};

export type NoteForm = {
  title: string;
  description: string;
};

export type NoteFormErrors = {
  title: string;
  description: string;
};
